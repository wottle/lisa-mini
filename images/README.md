# Images

Drop photos and renders here:

- `hero.jpg` — main photo referenced from the top-level README
- Build/assembly photos referenced from `docs/ASSEMBLY.md`
- Render angles (front, 3/4, side profile, rear) for reference

Keep large source photos out of git history where possible (resize for web
before committing) — this repo has no LFS configured.
